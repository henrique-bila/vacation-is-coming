# Git publish target — safe to commit (no credentials).
# Set configured: true after creating your GitHub repository.

configured: true
remote_url: "https://github.com/henrique-bila/vacation-is-coming.git"
branch: main
remote_name: origin
